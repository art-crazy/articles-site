# Test Folder Rules

AI agents must not generate, rewrite, or expand tests in this folder unless the user explicitly asks for tests in the current message.

When asked to change product code, do not add tests proactively. Report manual checks instead.
